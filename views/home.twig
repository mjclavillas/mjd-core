<!DOCTYPE html>
<html>
<head>
    <title>{{ title }}</title>
</head>
<body>
    <h1>Welcome to {{ appName }}</h1>
    
    {% if username %}
        <p>Hello, {{ username }}!</p>
    {% else %}
        <p>Hello, Guest!</p>
    {% endif %}

    <small>Current Environment: {{ env }}</small>
</body>
</html>